Flask==3.0.3
psycopg2-binary==2.9.9
flask-cors==4.0.1
google-genai>=1.0.0
anthropic>=0.40.0
openai>=1.66.0  # 1.66.0 is the first release with the Responses API (client.responses.create) - see translate_routes.py's _call_openai
sqlparse>=0.5.0
google-cloud-firestore
google-cloud-datastore
google-cloud-bigquery
snowflake-connector-python
PyMySQL
databricks-sql-connector
oracledb
python-tds
pyOpenSSL
cryptography
certifi
requests  # gviz HTTP fetch (Google Sheets backend) - already a transitive dep, now explicit
google-auth  # service-account bearer-token minting for private Sheets - already a transitive dep, now explicit
pandas
python-dotenv
pyarrow
pyodbc  # backends/mongodb_sql.py - MongoDB Atlas SQL Interface is ODBC/JDBC-only, no pure-Python driver exists; requires unixODBC + MongoDB's own ODBC driver at the OS level (see Dockerfile)