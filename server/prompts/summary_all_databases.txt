You previously helped route a user's natural-language question to one or more databases, and real queries have now been run against each of them. You will be given the user's ORIGINAL question and, for each database that was queried, exactly one of the following as its outcome: its actual result rows (there can be many, not just one - each block below states the real total row count and, if that count was capped, how many of those rows you're actually being shown), a note that it had nothing relevant to contribute, or an error explaining that querying it failed. Each database's results block below is labeled with its own [index], starting at [0] - use these SAME indices, as strings, to key your response.
Respond with ONLY a single JSON object - no markdown code fences, no other text before or after it - shaped exactly like this:
{"label": "...", "per_database": {"0": "...", "1": "...", ...}, "cross_database": "..." or null, "visualizations": {"1": {...}, ...}}
CRITICAL, before anything else: every string value in this JSON - the label, AND every per-database paragraph, AND the cross_database paragraph if you write one - MUST be written in the SAME LANGUAGE as the user's original question, never the language of the database/table names or of the results data you're given, and never any other language. This applies to every single string you write, not just the label.
"label" is a short (one to two word) section-heading label meaning "Results Summary" - in English this label is literally the phrase "Results Summary", but you must instead write it TRANSLATED into the SAME LANGUAGE as the user's original question. A response whose "per_database" is empty, or missing an entry for one of the indices given a results block above, is not a valid response - every such index must get its own entry.
Be concise and succinct throughout this entire response: every paragraph you write, in every field, should favor short, direct sentences that state the finding plainly - never elaborate prose, throat-clearing, hedging, or restating the question back before answering it.
"per_database" must have ONE separate short paragraph PER DATABASE, keyed by that database's own [index] (as a string, e.g. "0"), answering the original question using just that database's own results. Keep every paragraph brief and succinct - ONE sentence whenever possible, two short sentences at most - even if the underlying result set is large: this is a terse summary, not a report. If a database noted it had nothing relevant, say so in one short sentence rather than skipping it silently, so the user can see every database was actually considered. If a database's query instead failed with an error, don't just note that it failed - briefly explain, in plain language and in as few words as the explanation genuinely needs, what the error suggests actually went wrong (e.g. a permissions problem, a timeout, an ambiguous or unsupported request) and, if it's apparent from the error text, what could fix it, so the user understands the failure instead of only knowing that one occurred.
"cross_database" is a SEPARATE field for a short paragraph that spans MULTIPLE databases at once (e.g. comparing two of them, or a single figure combined across all of them) - only set it to a non-null string when the question genuinely asks for something like that; otherwise set it to JSON null. Keep it just as brief and succinct as the per-database paragraphs above - one or two short sentences, never a longer writeup. Never use this field to restate or recap the per-database paragraphs a second time - it is only for content that couldn't be attributed to any single database's own paragraph.
"visualizations" is an OBJECT deciding, INDEPENDENTLY for each eligible database result, whether it's ALSO shown as a chart instead of only a table. THE CHART AND THE TEXT MUST NOT BE DISJOINT, and this is enforced structurally, not just by instruction: a database's result being chartable (it appears in "Chartable result sets" below at all) is necessary but NOT sufficient - a chart for it is only actually shown to the user when that database's OWN "per_database" paragraph (or "cross_database", if the point is a cross-database one) contains the special inline link described just below, planted exactly at the point where you make the specific claim that chart illustrates. Adding a key to "visualizations" with no matching link anywhere in "per_database"/"cross_database" produces NOTHING for that key - it silently falls back to a plain table - so the link is not optional decoration, it is the one and only thing that makes a chart actually appear. The inline link uses ordinary Markdown link syntax, written directly inline in "per_database"/"cross_database" as regular flowing prose, wrapping a SHORT phrase - a handful of words, well under a full sentence, never a generic label like "chart" or "see graph", and never the whole sentence around it - that names the SAME specific point you are making right there: for example, if a paragraph is making the point that "Sales DB's Q3 revenue grew 18% over Q2", write that as `[Sales DB's 18% Q3 growth](chart:1)` in place of (or wrapped around) that same wording. The bracketed phrase is written in the SAME LANGUAGE as the paragraph it sits in; the parenthesized part is always the literal word "chart:" immediately followed by the EXACT string index you are ALSO using for this same result in "visualizations" below (the SAME [index] "per_database" is keyed by, e.g. "chart:1" for the key "1") - never any other URL, and never the "chart:" prefix without a valid index after it. This is the ONLY situation where a link belongs anywhere in "per_database"/"cross_database" - never write a real hyperlink for anything else. It is completely normal, and expected, for a database's result to be chartable yet get no link at all - e.g. because its paragraph never got around to discussing that particular result in enough specific detail, or discusses it only in passing. Never plant a link, or write a new sentence solely to plant one, just to justify charting something - the direction only ever runs "the paragraph already makes a specific point -> you link that exact phrase and add the matching \"visualizations\" entry", never the reverse. When more than one database's own point genuinely is made in its paragraph, each gets its own link at its own point in the text, and each can independently get its own chart in the same turn. "visualizations" must be an empty object {} whenever nothing here qualifies - see the "Chartable result sets" list given to you below: ONLY the keys shown there may ever appear in "visualizations", each copied EXACTLY as the quoted string index shown AND used identically after "chart:" in the paragraph's own inline link - never invent, renumber, or add a key for an index not listed there. For a result you decide isn't worth charting (including "chartable but its paragraph never specifically discusses it"), simply leave its key out of "visualizations" AND out of any inline link - do not include it set to null. For each key you DO include, its value must be an object shaped exactly like this: {"chart_type": "bar" or "line" or "scatter", "x_column": "<one column name>", "y_columns": ["<one or more column names>"], "series_column": "<one column name>" or null}. Use "bar" to compare values across categories, "line" when x_column is a time/sequence-like column and the question is about a trend over it, "scatter" to relate two numeric columns to each other. "x_column" is the column to place along the other axis from the values being measured; "y_columns" are the numeric column(s) actually being measured/compared - only ever pick columns explicitly marked "(numeric)" for that same index in "Chartable result sets" below, never a column marked "(text)". "series_column" optionally splits that chart into multiple series/groups (e.g. one line per region) - set it to null unless the data genuinely has a separate grouping column, distinct from x_column, that the question calls for breaking out. Every column name you write must be copied EXACTLY (case-sensitive) from that same index's own column list below - never invent, translate, or abbreviate a column name, and never mix columns from two different databases into the same chart. If you are ever unsure whether a chart genuinely helps for a given result, or unsure it truly matches something its paragraph specifically says, leave its key (and any link) out entirely - a plain table is always an acceptable, safe default for that one result, and there is no penalty for choosing it.
Respond with the JSON object only - no markdown tables, no bullet points, no headings, no commentary outside the JSON's own string values (this restriction is about "label"/"per_database"/"cross_database" specifically - "visualizations" is a structured object, not prose, and follows its own shape described above).
One final reminder, since it's the single most important rule above: the language of every string you write must match the user's original question, not the language of the schema/data.
