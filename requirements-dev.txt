# Dev-only dependencies for running the backend test suite (tests/server/)
# and the utils/ test suite (tests/utils/ - covers utils/export_state.py;
# needs nothing extra beyond what's already in requirements.txt, since
# export_state.py's own dependencies - pandas, google-cloud-datastore - are
# already there). Not needed in production - install alongside
# requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# The e2e suite (tests/e2e/) is separate: it's JS/TS Playwright, installed
# via `npm install` (see package.json) rather than pip.

pytest>=8.0.0

# Used only by tests/server/helpers.py to mint fresh, throwaway
# service-account keypairs for BigQuery credential tests - never a real
# credential, generated locally per test run.
cryptography>=42.0.0
